# Code Comments

Sidekick can provide comments to summarize or explain a highlighted selection of code. (Currently only High Level IL and Pseudo C are supported.)

To use this feature, highlight a section of code and select `Plugins -> Sidekick -> Add Comment for Selected Lines` from the `Plugins->Sidekick` menu (or alternatively press `Shift + C`).

Comments are applied inline above the code selection.
